# dependencies
node_modules

# build output
.next
out
build
next-env.d.ts
*.tsbuildinfo

# local run data
.data/

# secrets. The negation must come AFTER the pattern it exempts from,
# because the last matching rule wins.
.env
.env*
!.env.example
*.pem

# Workflow snapshots pulled from n8n for inspection. These embed credentials
# in node parameters, which is exactly how one reached a commit before.
gap.json
wf-*.json
wf.json

# vercel
.vercel

# macOS
.DS_Store
